spring:
  datasource:
    url: jdbc:mysql://localhost:3306/picTalk?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
cos:
  client:
    host: ${COS_HOST}
    secretId: ${COS_SECRET_ID}
    secretKey: ${COS_SECRET_KEY}
    region: ${COS_REGION}
    bucket: ${COS_BUCKET}

# DashScope 配置
dashscope:
  apiKey: ${DASHSCOPE_API_KEY}

# 百度云 AI
baidu:
  apiKey: ${BAIDU_API_KEY}